{
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "npx nodemon -w src build.js",
    "dev": "npx lite-server --baseDir=\"dist\""
  },
  "devDependencies": {
    "marked": "^4.0.12",
    "nodemon": "^2.0.15",
    "sass": "^1.49.9"
  }
}
