{
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "npx nodemon -w src build.js",
    "serve": "npx light-server -s build -p 8080"
  },
  "devDependencies": {
    "http-server": "^14.1.0",
    "light-server": "^2.9.1",
    "marked": "^4.0.12",
    "nodemon": "^2.0.15",
    "sass": "^1.49.9"
  }
}
